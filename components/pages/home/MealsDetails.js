import React from "react";
import { View, TextInput, Text, Pressable, Image, ScrollView } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function MealsDetails({ navigation }) {
  return (
    <ScrollView>

    </ScrollView>
  );
}
